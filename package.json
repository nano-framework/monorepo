{
  "name": "nano-monorepo",
  "version": "0.0.1",
  "main": "index.js",
  "author": "luis@nxtep.io",
  "license": "MIT",
  "scripts": {
    "build": "lerna run build",
    "bootstrap": "lerna bootstrap",
    "prebootstrap": "yarn run -s clean",
    "clean": "lerna run clean",
    "postclean": "lerna clean -y",
    "test": "jest --config jest.json"
  },
  "devDependencies": {
    "@types/jest": "^24.0.13",
    "@types/node": "^12.0.2",
    "jest": "^24.8.0",
    "lerna": "^3.14.1",
    "rimraf": "^2.6.3",
    "ts-jest": "^24.0.2",
    "ts-node": "^8.2.0",
    "typescript": "^3.4.5"
  }
}
